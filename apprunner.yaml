version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install uv
      - uv sync --frozen
run:
  command: uv run python main.py
  network:
    port: 8000
    env: SERVER_PORT
  env:
    - name: SERVER_PORT
      value: "8000"
    - name: PYTHONUNBUFFERED
      value: "1"
